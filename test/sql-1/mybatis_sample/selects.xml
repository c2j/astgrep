<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="demo.SampleMapper">
  <select id="findById" resultType="map">
    SELECT *
    FROM t1
    WHERE EXISTS (
      SELECT 1 FROM t2 WHERE t2.c1=t1.c1 AND t2.c2=1
    )
    AND t1.id > #{minId}
    ORDER BY t1.id
  </select>

  <select id="findIn" resultType="map">
    SELECT c1 FROM ${table}
    WHERE a IN (
      SELECT b FROM t2 WHERE t2.c1=t1.c1 AND t2.c2=1
    )
    ORDER BY t1.id
  </select>


  <select id="findIn" resultType="map">
    SELECT * FROM t1
   
  </select>

</mapper>

