rules:
- id: explain-metavar_cond
  patterns:
    - pattern: foo($X)
    - metavariable-comparison:
        metavariable: $X
        comparison: $X >= 10
  message: found it
  languages: [python]
  severity: WARNING
