# Simple Python test for basic pattern matching

def test_function():
    # Function calls
    eval("dangerous code")
    exec("more dangerous code")
    print("safe code")
    
    # String literals
    message = "hello world"
    greeting = 'hello world'
    
    # Numeric literals
    x = 42
    y = 3.14
    z = "42"  # This is a string, not a number
    
    # More function calls
    eval(user_input)
    evaluate("this should not match eval")
    
    return True

class TestClass:
    def method(self):
        eval(self.data)
        return eval("result")
